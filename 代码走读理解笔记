这套代码是看过所有“绕过cdn找真实ip” 中可以说最好的代码。
思路如下：
（1）根据域名找到所有的子域名
（2）找到主域名、子域名对应的ip
（3）根据子网掩码CIDR方式（127.0.0.1/30），找到上述所有ip同一C段的ip.
（4）找到默认port
（5）根据3中的ip + 4中的port,以及根据主域名请求的UA/COOKIE等信息，挨个发送请求
（6）然后比对 5中响应结果的长度跟主域名返回结果的长度比对，如果长度一致，则认为此ip是real ip.

注： 可以结合  https://github.com/zhanglvmeng/xcdn/  整合成一套比较全面的检测框架。
